set $conf_home /home/sc/.config/i3

# Font for window titles. Will also be used by the bar unless a different font is used in the bar {} block below.
font pango:firacode nerd font 11

include $conf_home/base.conf
include $conf_home/sof_init.conf
include $conf_home/sof_bind.conf
include $conf_home/mouse.conf
